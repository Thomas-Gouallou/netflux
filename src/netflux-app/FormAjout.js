import React, { Component } from 'react';
import { withRouter, Link } from 'react-router-dom';
import { DataService } from './DataService';

export class FormAjout extends Component {
    render() {
        return(
            
        )
    }
}

export default withRouter(FormAjout)